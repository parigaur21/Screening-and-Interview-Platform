FROM node:18-alpine
WORKDIR /app

# Ingest and cache npm modules
COPY package*.json ./
RUN npm ci --only=production

# Copy backend source modules
COPY . .

# Expose API runtime port
EXPOSE 5000

ENV NODE_ENV=production

# Boot server
CMD ["node", "src/server.js"]
